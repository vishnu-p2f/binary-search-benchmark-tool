{
  "name": "C++ Dev Environment",
  "image": "mcr.microsoft.com/devcontainers/cpp:ubuntu-22.04",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libspdlog-dev",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools"
      ]
    }
  }
}
